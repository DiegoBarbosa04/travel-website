// import type { Request, Response } from "express";
// import { searchLocationsService } from "../services/location.service";
// import { searchLocationSchema } from "../schemas/location.schema";

// export const searchLocations = async (req: Request, res: Response) => {
//   const { keyword } = searchLocationSchema.parse(req.query);

//   const locations = await searchLocationsService(keyword);

//   res.json(locations);
// };
